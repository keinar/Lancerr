export function GigSeller() {

    // const gigs = useSelector(storeState => storeState.gigModule.gigs)

  

    return (
        <div>
            <h3>Profile Setup and Personal Info(Seller page)
 <br/> Create a GIG</h3>
            <main>

            </main>
        </div>
    )
}